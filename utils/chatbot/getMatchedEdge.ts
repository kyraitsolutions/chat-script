// import { TChatbotEdge } from "@/types/chat-bot.type";

// type TMatchedEdgeProps = {
//   edges: TChatbotEdge[];

//   sourceHandle?: string;
// };

// export const getMatchedEdge = ({ edges, sourceHandle }: TMatchedEdgeProps) => {
//   if (!sourceHandle) {
//     return edges[0];
//   }
//   return edges.find((edge) => edge.sourceHandle === sourceHandle);
// };
